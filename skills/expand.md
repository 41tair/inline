# expand

Expand the selected region into a fuller version guided by the user's brief prompt.
Preserve the original intent, structure, and important details unless the prompt explicitly asks for a different direction.
Return a complete replacement for the selected region, not just an appended continuation.
